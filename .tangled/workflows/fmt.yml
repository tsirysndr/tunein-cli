when:
  - event: ["push", "pull_request"]
    branch: ["main"]

engine: microvm
image: nixos

dependencies:
  - cargo
  - rustc
  - rustfmt
  - gcc
  - pkg-config
  - alsa-lib

steps:
  - name: "cargo fmt"
    command: |
      cargo fmt --all --check

